---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: containercheckpointcontents.checkpointing.zacchaeuschok.github.io
spec:
  group: checkpointing.zacchaeuschok.github.io
  names:
    kind: ContainerCheckpointContent
    listKind: ContainerCheckpointContentList
    plural: containercheckpointcontents
    shortNames:
    - ccc
    singular: containercheckpointcontent
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ContainerCheckpointContent is the Schema for the containercheckpointcontents
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContainerCheckpointContentSpec defines the desired state
              of ContainerCheckpointContent.
            properties:
              checkpointPath:
                type: string
              containerCheckpointRef:
                type: string
              containerName:
                type: string
              creationTimestamp:
                format: date-time
                type: string
              namespace:
                type: string
              nodeName:
                type: string
              podName:
                type: string
            required:
            - checkpointPath
            - containerCheckpointRef
            - containerName
            - creationTimestamp
            - namespace
            - nodeName
            - podName
            type: object
          status:
            description: ContainerCheckpointContentStatus defines the observed state
              of ContainerCheckpointContent.
            properties:
              message:
                type: string
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
